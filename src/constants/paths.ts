const PATHS = {
  HOME: '/',
  LOGIN: '/login',
  VERSION: '/version-info',
  SUPPORT: '/support',
  ITEMS: '/items',
  WAREHOUSES: '/warehouses',
  ZONES: '/zones',
  IMPORT: '/import',
} as const;

export default PATHS;
