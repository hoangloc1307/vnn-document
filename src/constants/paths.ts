const PATHS = {
  HOME: '/',
  LOGIN: '/login',
  VERSION: '/version-info',
  SUPPORT: '/support',
  ITEMS: '/items',
  WAREHOUSES: '/warehouses',
  IMPORT: '/import',
} as const;

export default PATHS;
